# CourseStack · 参考图还原重构

本轮视觉依据：`/home/dax/Documents/project.png`。不是继续扩写课程，而是把既有课程带入同一套真实的教材界面。

## 实际范围

- 13 个课程首页全部重建为静态可读的课程总览，不再分成 dashboard / CS168 portal / MIT 特例三种产品。
- 528 个内容路由全部接入同一阅读契约，包含 307 份主讲义、主工作簿以及历史补充路由；没有只改示例页。
- EECS498 的 6 份动态复盘预渲染为完整静态正文，脚本失败时仍有内容。
- 原始资料、题号、个人实现归属、课程学期与 sourceStatus 保留。没有照抄参考图中错误的课程名称或虚构学习进度。

## 从审计到修改

| 优先级 | 旧问题 | 实际修复 |
| --- | --- | --- |
| P1 | 参考图有统一搜索顶栏，旧页却有多种导航 | 全站统一中文顶栏、课程 / 关于 / 社区与真实全文目录搜索 |
| P1 | 课程首页被当成目录或 dashboard | 静态身份区、继续学习、进度环、阶段节点图、三种学习方式 |
| P1 | 只有 CS168 获得完整首页设计 | 13 门课共用生成器，各自拥有独立阶段与真实入口 |
| P1 | 右侧只有长 TOC，没有语义注释 | TOC 移到左侧；右侧摘取当前页实际的模型、不变量、误区或练习 |
| P1 | CS267 / 6.1810 内层 TOC 继续占列 | 隐藏重复目录并解除内层双列，让正文真正恢复宽度 |
| P1 | 动态增强晚于 reader 初始化 | 对新增教学 section 做有限观察，更新目录与注释，不反复观察自身 |
| P1 | 不同课程 quiz 行为分叉 | 统一选择 → 检查 → 诊断；兼容数值索引、a–d 和逐按钮答案 |
| P1 | 数学公式 HTML 存在但浏览器报错 | 修复 11 个页面的括号、矩阵环境、绝对值和下标语法 |
| P1 | 统一界面只在脚本执行后出现，首屏会闪旧布局 | 全部内容页在静态 HTML 中声明教材壳层；脚本只做渐进增强，旧导航在增强成功后才退场 |
| P1 | 参考图的分层插画缺少语义，仍像装饰图 | 把五层标成使用者、应用、协议、基础设施与更广阔的世界，让插画参与平台叙事 |
| P2 | 首页缺少参考图中的实物插图与主题入口 | 增加分层 / 轨道地球资源、精选课程、主题筛选 |
| P2 | 代码块缺少统一阅读细节 | 语言栏、复制反馈、只给真实代码添加行号，复制不包含行号 |
| P2 | 右侧注释可能套上网络模板 | 从当前页的真实块摘取；不把表格单元拼成“概念解释” |
| P2 | EECS498 项目页仍有英语 UI 和紫色旧模板 | 23 个阶段标题及契约 / 提示 / 复盘中文化，统一轻量语义组件 |
| P2 | 桌面布局靠 CSS 隐藏大量旧检查器 | reader 模式不再构建无用的旧 rail、tabs 和 inspector |

## 共享实现

- `assets/course/platform.css` / `platform.js`：全站 token、统一顶栏、搜索、首页共用元素。
- `assets/course/course-home.js`：在完整静态课程目录上增强本机进度、续读和过滤。
- `assets/course/reference-reader.css` / `reader.js`：左目录、正文、右注释及窄屏降级。
- `assets/reference-home.css`：平台首页的比例、插图、精选课程、主题与学习方式。
- `assets/course/quiz.js`：共用可初始化的测验引擎；数学与推理工作簿复用它。
- `tools/build-course-homes.mjs`：构建全部首页，`--check` 检查是否同步。
- `tools/build-review-labs-static.mjs`：直接复用 EECS498 生产 renderer 预渲染，不建立第二份内容规范。
- `tools/apply-reference-layout.mjs`：覆盖全部内容路由，并产生逐页清单。

## 设计边界

参考图是 1448 × 1086 的四屏设计板，不是一个可直接当作浏览器 viewport 的页面。本轮保持其结构、色彩、字体层级、节点图、三栏正文与组件关系；真实课程内容长度和用户本机进度会产生合理差异。

学习进度仅使用 `coursestack.learning.v1`，由用户确认完成，不读取编写状态充当学习完成度。没有账户系统，因此不添加假登录按钮或假同步状态。

插图资源：`assets/illustrations/system-layers.png` 与 `network-globe.png`，由内置 imagegen 依据原图分别制作成独立生产资源；不再从合图裁切。提示词核心为：保留五层浅蓝半透明板与浅蓝轨道地球，不生成页面、不生成文字、不生成新视觉风格。图标使用现有 Tabler 图标体系与其许可证。

## 验证记录

真实浏览器逐路由结果写入 `.course-build/reference/render-results.json`；页面清单写入 `page-manifest.json`。交互验证位于 `interaction-results.json`，截图和归一化对照位于同目录。

已实测：全站搜索、主题筛选、课内搜索与空状态、渐进提示、解析、先选后检查、笔记刷新恢复、DV 逐轮推演、完成标记、续读、进度环、代码复制及数学推演。1280 / 1024 / 768 / 500 CSS px 的响应式交互另做验证。

最终状态与完整计数见根目录 `design-qa.md`。不把自动检查通过等同于全课程内容已达到 GOLD；本轮没有对 528 页作新的教学深度认证。
