# 中文教材界面 · 设计 QA

参考图：`/home/dax/Documents/project.png`（1448 × 1086，四屏概念板）。文字要求优先：单栏正文＋右侧目录，静态站点直接实现。

实现截图：`.course-build/reader-qa/{home,course,lecture,discussion,project}-{1440,768,500}.png`。代码局部：`.course-build/reader-qa/project-code.png`。Firefox 桌面截图为 1440 × 914，CSS viewport 1440 × 914，density 1；另测 768、500 CSS px。概念板的各屏为缩小示意，因此按对应区域的层级、排版、组件对照，不声称逐像素复制。

参考板与上述实页在同一图像输入中打开并比较。完整视图检查结构、留白、目录与阅读层级；代码局部检查语言栏、复制按钮、语法颜色和注释。使用真实课程资料和学习记录，不复制图中的示意数据。

## 迭代发现与修复

1. P1：旧工作台抢占阅读空间。浅色顶栏、单栏正文及右侧目录取代 reader 页旧状态面板；实验回到内容原位置。
2. P2：旧 CSS 强制大标题、绿色正文和过重来源块。映射 reader tokens，统一标题节奏，将首屏版本说明收为轻量折叠说明。
3. P2：窄屏目录展开阻碍进入正文。改为默认折叠，桌面保持可见。
4. P2：KaTeX 直接转文字会重复公式。右侧使用短推演提示，正确公式留在正文。
5. P2：旧 Prism token 在浅底对比不足，语言栏未铺满。提高 token 对比并补齐通栏 header；最终代码局部已重新对照。
6. P1：内容生成状态被当作学习进度。改为实际阅读与手动确认完成，首次访问显示零进度。

首次截图为目录内无宽度后缀的版本；最终修复证据为带宽度后缀的截图及 project-code.png。

## 五项设计检查

- 字体：中文系统字体优先，正文 16px/1.9，标题 34/25/18px，辅助列 13px；品牌为简洁衬线字标。
- 空间：最大 1260px 内容框架，正文段落最大 790px，宽表格与代码可展开；15 个浏览器状态无页面横向溢出。
- 配色：白底、蓝灰文字、细边框；蓝色交互、浅绿不变量、暖色误区/失败；无新增渐变装饰。
- 图像：保留教学拓扑，使用真实 Tabler 标准图标并保存许可证。参考中的装饰地球与插画按文字要求省略，没有用代码绘图代替装饰资产。
- 文案：平台、课程入口、练习标题、提示/解析、状态标签和复制中文化；源码标识符及原课程技术术语保留。

## 功能验证

Firefox WebDriver 实际点击通过：提示、解析、quiz、trace、表格面板、完成标记、复制代码。草稿刷新保留、课程/首页续读和搜索空状态通过。机器结果：`.course-build/reader-qa/results.json`。全站 14 项检查通过。

未执行跨浏览器矩阵、实体手机或浏览器控制台日志收集；不声称没有任何控制台消息。原课程内容未重新做学科事实审计。

P3 后续：逐步把剩余英文技术标题在模板中本地化，接入其他课程时削减旧 CSS 适配规则。无剩余阻碍当前范围使用的 P0/P1/P2 问题。

final result: passed
