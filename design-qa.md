# CourseStack 参考图还原 · 设计 QA

## 视觉真值与对照方法

- 视觉真值：`/home/dax/Documents/project.png`，1448 × 1086，包含平台首页、课程首页、教材正文与组件四个设计屏。
- 实现截图：`.course-build/reference/final-home-current.png`、`final-course-current.png`、`final-lesson-current.png`，Firefox 以 1440 × 1050 请求视口渲染，实际内容视口 1440 × 964。
- 同屏对照：`.course-build/reference/comparison-detail-final.png`。参考图按原始面板裁取，真实页面按同一面板宽度归一化；比较信息层级、相对比例、密度、留白、线条、颜色与组件节奏，不把设计板中的虚构学习数据照搬为真实状态。
- 聚焦复核：另查看 CS168 Discussion 03、Project 2、CS336、6.102、6.1810、CS267 与 EECS498 Review，覆盖普通讲义、数学页、工作簿、代码复盘、动态 trace 和特殊旧布局。

## 三轮可见差异与修复

1. 初次对照发现课程首页存在 dashboard / portal / MIT 特例三套产品、正文列偏窄、CS336/CS267/6.1810 有重复 TOC。修复为同一课程身份区、阶段地图和左目录—宽正文—右语义注释布局，并移除可见旧目录。
2. 第二次对照发现首页与课程页字号、最大宽度和插画占比低于参考；首页分层图只是装饰。最大内容宽度调到 1360px，重做标题与卡片比例，并补齐“使用者—应用—协议—基础设施—更广阔的世界”语义标注。
3. 最终对照发现双图裁切不利于响应式和资产维护。用 ImageGen 按参考视觉分别生成 `system-layers.png` 与 `network-globe.png` 两张独立生产资源，取消合图裁切；当前同屏对照没有剩余 P0、P1 或 P2 视觉问题。

## 全量范围

- 13 个课程首页全部由同一生成器产出，但使用各课程自己的名称、学校、学期、阶段问题、讲义与实践数据。
- 528 个内容路由全部接入共享教材壳层；其中包括 6.102 自学入口、CS267 项目索引和 EECS498 六个静态预渲染 Review。
- 所有内容页的静态 HTML 已直接声明 `cs-platform reader`，避免首屏先出现旧布局；脚本只增强搜索、目录、进度和练习交互。
- 右栏只呈现当前文章中真实存在的心智模型、不变量、易错点、历史说明或来源，不套课程无关的模板文本。

## 浏览器与交互验证

- Firefox 全路由渲染：542 条公开路径 × 桌面 / 窄屏两种状态 = 1084 次状态检查，`issues=0`。
- 检查项：横向溢出、统一 header 数量、唯一可见 H1、断图、KaTeX 错误、共享目录挂载与旧 TOC 可见性。
- 课程首页当前版本复查：13 门课程 × 1440 / 1000 / 500px = 39 个状态，`issues=0`。
- 学习交互：14 项全部通过，包括全站搜索、主题筛选、课内搜索、渐进提示、解析、先选择再检查、笔记持久化、DV trace、完成/续读/进度环、代码复制与行号、数学检查，以及 1280 / 1024 / 768 / 500px 响应式。
- 内容与链接：13 门课程、307 节讲义、168 份实践、12,922 个本地引用通过完整性检查；课程专项深度 lint 全部通过。

## 有意保留的真实差异

- 不伪造参考图中的 45% 学习进度、账号登录或云同步；进度来自当前浏览器中用户明确确认的完成记录。
- 不照抄参考图中示意课程名；首页与课程页展示仓库中的真实课程数据与 sourceStatus。
- 参考图是四屏设计板，不是单个浏览器页面；长课程目录按真实内容自然向下延伸。

final result: passed
