(function () {
  const bank = window.MIT61810PracticeBank = window.MIT61810PracticeBank || {};
  const Q = (prompt, correct, wrongA, wrongB, why, diagnosisA, diagnosisB, followUp) => ({
    prompt, options: [correct, wrongA, wrongB], answer: 0,
    explanations: [why, diagnosisA, diagnosisB], followUp
  });
  const F = (prompt, correct, wrongA, why, diagnosisA) => Q(prompt, correct, wrongA, null, why, diagnosisA, null);

  bank['01-introduction'] = {
    problem: '应用想要方便地共享硬件，但彼此又不能互相破坏；操作系统怎样同时提供抽象、隔离与复用？',
    map: ['应用请求', '系统调用边界', '内核机制', '受保护的硬件资源'],
    before: [
      ['函数调用和系统调用最关键的区别是什么？', '普通函数调用通常不改变特权级；系统调用通过受控入口从用户态进入内核态。'],
      ['进程和程序文件是同一个对象吗？', '不是。程序是静态字节；进程是执行中的地址空间、寄存器、内核状态与资源引用。'],
      ['为什么“方便”与“隔离”会冲突？', '共享物理资源最方便，但若没有边界，一个错误或恶意程序就能改写他人的状态。']
    ],
    mental: '操作系统不是一组库函数，而是处在特权边界上的资源裁判：应用只能通过受检查的接口请求共享硬件。',
    reasoning: ['对象：用户程序、系统调用接口、内核、设备与持久化状态。', '最小例子：两个程序都调用 write；内核必须决定各自能写哪个 fd、何时写设备。', '机制链：用户请求 → trap → 内核验证参数与权限 → 操作资源 → 返回结果。', '为什么不是普通库：库无法阻止调用者绕过检查并直接访问硬件。'],
    checks: [
      ['操作系统的目的', '抽象与保护', Q('若把文件系统做成普通用户库，哪项能力最难由这个库单独保证？', '阻止另一个进程绕过库直接改写磁盘', '把路径字符串解析成目录项', '把常用操作封装成函数', '保护依赖特权边界和硬件访问控制。', '解析是机制工作，用户库也能实现；你忽略的是强制性。', '封装改善易用性，但不提供不可绕过的隔离。', F('同理，为什么内存分配器库不能单独隔离两个恶意进程？', '库不能控制页表与 CPU 访问权限', '因为 malloc 只能分配小对象', '页表与特权指令才形成强制边界。', '对象大小不是隔离的根因。'))],
      ['介绍 UNIX 系统调用', '接口与实现', Q('应用调用 read(fd, buf, n) 时，哪项属于内核必须验证的对象关系？', 'fd 是否属于该进程，buf 是否指向可写用户内存', 'read 的 C 函数名是否足够短', '该应用是否链接了 libc 的全部符号', '内核不能信任用户传入的句柄和指针。', '命名风格不影响保护。', 'libc 是包装层，不是权限依据。', F('若 buf 跨过一个未映射页，内核应怎样理解它？', '这是需要安全失败或分段复制的用户地址范围', '因为起始地址有效，所以整个范围都有效', '必须验证整个访问范围而非只看首地址。', '这把一个点的有效性误当成整个区间有效。'))]
    ],
    transfer: Q('某新接口把“打开文件并读取”合成一个系统调用。它一定比两个系统调用更好吗？', '不一定；边界往返减少，但接口、权限语义与组合性也会改变', '一定更快，因为系统调用数更少', '一定更安全，因为内核代码更多', '系统设计要同时看成本、语义与可组合性。', '次数只是成本的一部分，不是完整结论。', '代码所在位置不自动决定安全性。'),
    open: [
      ['不用“管理资源”四个字，解释操作系统为什么需要特权级。', '说明应用不能被信任、敏感硬件操作必须不可绕过、内核通过受控入口验证请求。'],
      ['举一个“接口相同但内核实现可以不同”的例子。', '例如 read 接口不变，底层可以从磁盘缓存、设备、网络文件系统或内存文件系统取数据。']
    ],
    explain: ['系统调用为什么不是普通函数调用？', '抽象、复用与隔离分别解决什么问题？', '为什么内核必须验证用户指针？', '进程与程序文件为什么不是同一个对象？', '为什么 API 设计不能只看调用次数？'],
    bridge: ['Lab util · 从用户程序观察接口', '../labs/util.html#workbook-stage-tools', '用最小用户程序、管道和进程树把系统调用接口变成可观测行为。']
  };

  bank['02-c-in-xv6'] = {
    problem: '内核 C 代码直接操作地址和共享状态；怎样在缺少运行时保护时仍然准确推理对象、生命周期与位级表示？',
    map: ['C 表达式', '地址/布局', '生命周期', '内核状态变化'],
    before: [
      ['指针值和指针指向的对象有什么区别？', '指针值是地址；对象是该地址范围内具有类型、大小和生命周期的存储。'],
      ['数组参数进入函数后还保留完整数组类型吗？', '通常会退化为指针，因此函数无法仅从参数恢复数组长度。'],
      ['位掩码为何适合表示 PTE 权限？', '多个独立布尔属性可放入不同 bit，并用与、或操作检查或组合。']
    ],
    mental: '读内核 C 时不要只跟变量名，要同时跟四件事：地址指向谁、对象多大、谁拥有它、何时失效。',
    reasoning: ['对象：指针值、被指对象、分配区间、所有者与生命周期。', '最小例子：p 指向一个 struct；p+1 前进的是 sizeof(struct)，不是一个 byte。', '机制链：类型决定步长 → 地址运算定位对象 → 解引用读写存储。', '为什么不是“地址就是整数”：整数运算不会自动保留对齐、边界与对象语义。'],
    checks: [
      ['C 指针', '对象边界', Q('int *p 指向长度 4 的数组首元素。p + 3 表示什么？', '第 4 个 int 的地址', '首地址再加 3 个字节', '数组末尾之后的地址', '指针算术按所指类型大小缩放。', '你把 typed pointer 当成 byte pointer。', 'p+4 才是 one-past-end。', F('若先转成 char * 再加 3，步长是什么？', '3 个字节', '3 个 int', 'char 的大小为 1 byte。', '转换后指针的元素类型已改变。'))],
      ['常见 C 语言陷阱', '生命周期', Q('函数返回局部数组的地址，调用者稍后读取。最准确的判断是？', '指针悬空，行为未定义', '只要内容尚未被覆盖就始终正确', '编译器会自动把数组移到堆上', '栈帧结束后对象生命周期已终止。', '偶尔看似可用不能让未定义行为变正确。', 'C 不会自动逃逸分配。', F('返回 kalloc 得到的地址为何不同？', '对象生命周期由显式释放决定，不随函数返回结束', '因为内核函数没有栈', '动态分配对象不属于被调用者栈帧。', '内核函数同样使用栈。'))]
    ],
    transfer: Q('两个指针数值相同，能否据此断言它们始终代表同一有效对象？', '不能；还要看地址空间、类型、映射和对象生命周期', '能，地址相同就是对象相同', '不能，因为 C 指针从不允许比较', '内核上下文会改变地址解释。', '你忽略了页表和生命周期。', '同一数组内的指针比较是允许的。'),
    open: [
      ['用所有权解释一次典型 use-after-free。', '对象已归还分配器并可能被新用途复用，但旧指针仍被当作原对象使用。'],
      ['为什么内核代码中的整数溢出可能变成安全问题？', '长度或边界计算绕回后，检查与实际复制范围不一致，可能越界访问。']
    ],
    explain: ['typed pointer 的算术步长从哪里来？', '数组、指针与数组长度的关系是什么？', '栈对象和动态分配对象的生命周期差异？', '位掩码如何组合与检查标志？', '为什么“这次运行没崩”不能证明 C 代码正确？'],
    bridge: ['Lab util · 在小程序中校准 C mental model', '../labs/util.html#workbook-stage-programs', '先用短生命周期、明确 fd 所有权的程序验证指针、字符串和进程接口。']
  };

  bank['03-os-design'] = {
    problem: '不可信应用与共享硬件之间必须有一道不可绕过的边界；这道边界由哪些硬件与软件对象共同形成？',
    map: ['用户态', 'trap 边界', '内核态', '硬件保护'],
    before: [
      ['CPU mode 解决什么问题？', '限制当前代码能执行哪些特权指令和访问哪些受保护资源。'],
      ['页表只负责地址翻译吗？', '还编码读写执行与用户可访问等权限，是隔离机制的一部分。'],
      ['宏内核与微内核的区别主要是代码量吗？', '核心是服务运行在哪个保护域、如何通信与故障隔离。']
    ],
    mental: '隔离不是某一个 if 检查，而是 CPU mode、页表、trap 入口和内核验证共同闭合的一条信任链。',
    reasoning: ['对象：执行特权级、地址空间、入口表、内核对象句柄。', '最小例子：用户写磁盘寄存器；CPU mode 必须先禁止，系统调用再提供受检查替代。', '机制链：硬件拒绝直接操作 → trap 进入可信代码 → 内核检查调用者与参数。', '为什么不是只靠编程规范：恶意代码不会自愿遵守规范。'],
    checks: [
      ['隔离：为什么需要受保护的内核？', '不可绕过性', Q('若用户态不能执行特权指令，但可任意改写内核页表，隔离是否成立？', '不成立；页表控制权本身就是特权资源', '成立；只要特权指令被禁止即可', '成立；因为页表只是性能缓存', '保护链任一环可绕过都会破坏隔离。', '你把必要条件误当成充分条件。', '页表直接决定可访问的物理内存。', F('若页表安全但 trap 入口可跳到任意内核地址呢？', '仍不成立，受控入口被绕过', '成立，因为已经进入内核态', '进入哪里同样必须受硬件约束。', '“进入内核”不等于“按内核协议进入”。'))],
      ['宏内核 vs. 微内核', '保护域 trade-off', Q('把文件系统移到用户态服务，最直接得到与付出的分别是什么？', '故障隔离增强；IPC 与跨域交互成本增加', '磁盘一定更快；代码一定更少', '不再需要内核；不再需要权限检查', '保护域变化带来隔离与通信的交换。', '性能与代码量都不是无条件结论。', '微内核仍需调度、IPC、内存保护等核心机制。', F('微内核文件服务崩溃为何不必带崩核心内核？', '它在独立地址空间，错误传播边界更窄', '因为用户态代码没有 bug', '地址空间隔离限制直接内存破坏。', '运行位置不会消除 bug。'))]
    ],
    transfer: Q('“内核越小越安全”是否是严格定律？', '不是；更小可缩小可信计算基，但接口、实现质量和攻击面仍需具体分析', '是，只需比较代码行数', '不是，因为用户态服务无需安全', '这是工程倾向，不是只由规模决定的恒等式。', '行数不能代表全部复杂度与可利用性。', '用户态服务同样处理不可信输入。'),
    open: [
      ['画出一次受保护系统调用必须经过的信任链。', '用户指令→硬件 trap→固定入口→保存状态→内核校验→资源操作→受控返回。'],
      ['为什么“系统调用接口很小”通常有助于安全审计？', '跨信任边界的输入和状态转换更少，更容易穷举验证；但仍不是充分条件。']
    ],
    explain: ['CPU mode、页表和 trap 各自保护什么？', '为什么隔离必须不可绕过？', '宏内核与微内核真正划分的对象是什么？', '可信计算基为什么不等于总代码量？', '一次系统调用的哪些输入不能信任？'],
    bridge: ['Lab syscall · 把保护边界变成代码路径', '../labs/syscall.html#workbook-stage-trace', '沿用户 wrapper、trap、dispatch 和返回路径追踪一次受控状态转换。']
  };

  bank['04-organization'] = {
    problem: '把 OS 服务拆进多个保护域能缩小故障半径，但每次协作都要付出跨域通信；怎样理解这项结构性 trade-off？',
    map: ['保护域划分', 'IPC', '服务组合', '隔离/性能'],
    before: [
      ['IPC 和普通函数调用的额外工作来自哪里？', '地址空间切换、调度、消息复制或映射、权限验证。'],
      ['故障隔离的对象是什么？', '限制一个组件的错误可以直接破坏哪些状态和服务。'],
      ['性能比较为什么必须声明 workload？', '调用粒度、缓存、消息大小与并发度会改变跨域成本占比。']
    ],
    mental: '微内核不是“把代码删掉”，而是把服务移到独立保护域，再用 IPC 重新连接原来的调用图。',
    reasoning: ['对象：核心机制、用户态服务器、client、IPC endpoint。', '最小例子：exec 需要进程管理、文件服务和地址空间服务协作。', '机制链：client 请求 → 多个受保护服务交换消息 → 组合出高层语义。', '为什么不是免费隔离：原来函数调用的边变成跨域消息。'],
    checks: [
      ['示例：L4 上的 exec()', '调用图', Q('一次 exec 经过多个用户态服务，最容易被低估的系统成本是什么？', '跨保护域的多次 IPC 与调度依赖', '可执行文件的字符数', 'C 函数调用的栈帧大小', '服务拆分把内部调用边变成显式通信。', '输入名称不决定系统成本。', '本地栈帧不是主要新增项。', F('把多个细粒度请求批成一个消息主要减少什么？', '固定 IPC 往返与调度开销', '文件总字节数必然减少', '批处理摊薄每次跨域固定成本。', 'payload 可能完全相同。'))],
      ['性能评估', '结论边界', Q('某微基准显示 IPC 快 2 倍，能否直接推出完整 OS 快 2 倍？', '不能；端到端路径还包含服务工作、缓存与调度', '能；IPC 是唯一成本', '能；微基准总能代表真实负载', '局部机制提升需看它在总路径中的占比。', '这是把一个组件当成整个系统。', '代表性必须由 workload 证明。', F('如果 IPC 只占总时间 10%，即使它无限快，最大收益是否有限？', '是，剩余 90% 形成上界', '否，局部加速可无限放大', 'Amdahl 约束端到端收益。', '你忽略了未优化部分。'))]
    ],
    transfer: Q('将设备驱动移出内核一定改善可靠性吗？', '通常缩小内核故障面，但协议、恢复与共享硬件状态仍可能失败', '一定，因为用户态不会崩溃', '一定变差，因为 IPC 必然不可靠', '结构能改变故障传播，不会消灭故障。', '用户态同样会有 bug。', 'IPC 可以可靠实现，关键在完整系统设计。'),
    open: [
      ['用一张调用图解释微内核为什么需要高性能 IPC。', '服务边界越多，原本本地的调用边越多地落到 IPC 热路径。'],
      ['给一个值得用隔离换性能的组件例子，并说明理由。', '例如复杂驱动：输入不可信、bug 多、崩溃半径大；隔离收益可能超过通信成本。']
    ],
    explain: ['微内核保留和移出了哪些对象？', '保护域划分如何改变调用图？', '为什么 IPC 微基准不能代替端到端评估？', 'Amdahl 如何限制 IPC 优化收益？', '隔离为什么不等于故障自动恢复？'],
    bridge: ['Lab syscall · 观察边界固定成本', '../labs/syscall.html#workbook-stage-policy', '在 xv6 的系统调用边界上加入策略，再区分机制正确性与跨域成本。']
  };

  bank['05-page-tables'] = {
    problem: '每个进程都想从地址 0 开始拥有连续内存；硬件如何把同一个虚拟地址安全地解释为不同物理页？',
    map: ['虚拟地址', '多级页表/PTE', '物理页与权限', 'TLB'],
    before: [
      ['4 KiB 页为什么留下 12 位 offset？', '\(4096=2^{12}\)，页内字节由低 12 位选择。'],
      ['映射与分配是同一个动作吗？', '不是。分配获得物理页；映射把虚拟页、物理页和权限写入页表。'],
      ['TLB 缓存的是什么？', '最近的虚拟页到物理页及权限的翻译，而不是用户数据本身。']
    ],
    mental: '页表不是“内存内容表”，而是一棵由地址位索引的受保护映射：叶 PTE 同时回答去哪里和能做什么。',
    reasoning: ['对象：VA、页内 offset、VPN 索引、PTE、PPN、权限位、TLB entry。', '最小例子：同一 VA 0x1000 在进程 A/B 的根页表下可映射到不同 PA。', '机制链：satp 找根 → 逐级索引 → 叶 PTE 给 PPN/权限 → 拼回 offset。', '为什么不是单级巨表：稀疏地址空间会为大量未使用 VPN 支付固定内存。'],
    checks: [
      ['页表：寻址的间接层', '地址拆分', Q('4 KiB 页中，两个 VA 只在低 12 位不同。若映射有效，它们会怎样？', '命中同一物理页内的不同字节', '一定命中两个不同物理页', '必须走不同的根页表', '低 12 位是原样保留的页内 offset。', '你把 offset 当成 VPN。', '根页表由地址空间决定，不由 offset 决定。', F('若只更改 VPN 的某一索引位，offset 会怎样？', '保持不变', '被清零', '地址翻译拼接 offset，不修改它。', '页边界变化不等于 offset 自动清零。'))],
      ['三级页表：节省空间', '稀疏结构', Q('多级页表节省空间的直接原因是什么？', '未使用的虚拟地址范围无需分配下级页表页', '每个 PTE 从 8 字节压缩成 1 bit', '它取消了物理页', '树只展开实际映射覆盖的分支。', 'PTE 大小没有因此变成 1 bit。', '页表只描述物理页，不替代物理页。', F('一个地址空间突然稠密映射全部 Sv39 范围，多级结构仍会大幅省空间吗？', '不会，几乎所有分支都必须展开', '会，因为层数固定为 3', '收益依赖稀疏性。', '层数固定不代表节点数固定很少。'))]
    ],
    transfer: Q('修改一个 PTE 后，为什么仅写内存可能仍不足？', 'CPU 可能继续使用旧 TLB 翻译，需要相应失效/同步', 'PTE 只在磁盘上生效', 'MMU 不读取页表', '页表与翻译缓存是两个状态对象。', 'PTE 位于内存，不是仅在磁盘。', 'TLB miss 时 MMU 会遍历页表。'),
    open: [
      ['手推 Sv39 地址翻译时要依次写出哪些对象？', '根页表、三级 9-bit 索引、每级 PTE、叶 PPN、12-bit offset 与权限检查。'],
      ['为什么 PTE 的 W 位不是“软件建议”？', 'MMU 在每次访问上强制检查；不满足权限会产生 fault。'],
      ['解释“分配了页但忘记映射”和“映射到已释放页”分别会怎样。', '前者进程不可达且泄漏；后者产生 use-after-free/别名与隔离破坏。']
    ],
    explain: ['vocabulary 式地区分 VA、PA、VPN、PPN 与 PTE。', '三级页表为什么依赖地址空间稀疏性？', 'PTE 为什么同时承载位置与权限？', 'TLB 与页表为何可能暂时不一致？', '分配物理页与建立映射为什么不能混为一谈？'],
    bridge: ['Lab pgtbl · 亲手建立和检查映射', '../labs/pgtbl.html#workbook-stage-observe', '先从 tiny page table 读懂 PTE，再实现映射、打印与超级页生命周期。']
  };

  bank['06-syscall-entry'] = {
    problem: '系统调用要跨越特权级、页表和栈，但必须保留足够状态让用户程序像“函数返回”一样继续执行。',
    map: ['用户寄存器', 'ecall/trap', 'trampoline/trapframe', '内核处理与返回'],
    before: [
      ['trap 前 CPU 寄存器属于谁的执行状态？', '它们记录被中断用户线程的当前执行状态，必须保存才能恢复。'],
      ['为什么切换页表时代码不能突然消失？', '切换前后执行中的 PC 必须在两个页表下都有有效映射，trampoline 提供这座桥。'],
      ['kernel stack 和 user stack 能混用吗？', '不能。内核不能信任用户栈映射或内容，进入后应切到受保护内核栈。']
    ],
    mental: '系统调用入口是一场可逆的状态迁移：先把不可信上下文冻结到 trapframe，再建立可信内核执行环境。',
    reasoning: ['对象：sepc、sstatus、satp、sp、trapframe、trampoline、kernel stack。', '最小例子：ecall 时 PC 进 sepc，CPU 跳 stvec；其余通用寄存器仍需软件保存。', '机制链：硬件最小切换 → 汇编保存上下文/切页表栈 → C handler → 逆序恢复。', '为什么不是直接调用 C：进入瞬间还没有可安全使用的内核栈与完整保存状态。'],
    checks: [
      ['系统调用流程总览', '硬件与软件分工', Q('RISC-V trap 硬件是否自动保存全部通用寄存器？', '不会；入口汇编必须把它们保存到 trapframe', '会，所以 trampoline 只负责打印日志', '会保存到用户栈', '硬件只完成最小特权切换与少量 CSR 更新。', '你把 x86 风格或高级抽象误投射到 RISC-V。', '用户栈不可信且映射可能改变。', F('如果入口代码在保存 a0 前覆盖它，最直接丢失什么？', '用户传入的参数/原寄存器状态', '页表根地址必然丢失', 'a0 是用户上下文的一部分。', 'satp 位于 CSR，不等于 a0。'))],
      ['Trampoline：用户/内核的桥梁', '跨页表连续执行', Q('trampoline 为什么映射在用户和内核页表的同一虚拟地址？', '切换 satp 前后 PC 可继续取同一段入口/返回代码', '为了让用户直接修改内核代码', '为了减少 trapframe 大小', '页表切换不能让当前指令地址失去意义。', '用户映射可清除 PTE_U，存在映射不等于可由用户执行。', '映射位置与 trapframe 尺寸无关。', F('同一 VA 必须对应同一 PA 吗？', '对 trampoline 必须如此，才能执行同一代码字节', '不必，CPU 会猜测目标代码', '连续语义要求两侧映射到相同 trampoline 物理页。', '硬件不会自动猜测软件意图。'))]
    ],
    transfer: Q('把所有 trapframe 都放在一个全局可写页有什么核心风险？', '并发线程会覆盖彼此上下文，且所有权和隔离失效', '只会浪费一个 cache line', '系统调用会自动串行因此没风险', '保存区必须与被保存线程一一关联或正确同步。', '这是正确性而非仅性能问题。', '多核/抢占下系统调用并不全局串行。'),
    open: [
      ['不看代码，按顺序讲一次 user→kernel→user。', 'ecall、CSR 更新、stvec、保存寄存器、切 satp/sp、handler、准备返回、恢复页表/寄存器、sret。'],
      ['为什么 trapframe 既是数据结构又是协议？', '字段布局被汇编入口、C handler 和返回路径共同约定；任一方理解不同都会破坏恢复。']
    ],
    explain: ['trap 硬件自动做什么、没有做什么？', 'trapframe 保存哪些类别的状态？', '为什么必须切换到内核栈？', 'trampoline 为什么要双重映射？', '返回路径为何基本逆转入口路径？'],
    bridge: ['Lab traps · 验证可逆状态迁移', '../labs/traps.html#workbook-stage-assembly', '从汇编寄存器追踪开始，再实现 backtrace 与用户 alarm 的保存/恢复协议。']
  };

  bank['07-interposition'] = {
    problem: '要限制不可信程序，为什么“拦截系统调用”看似完美却很容易被路径、并发和 TOCTOU 绕过？',
    map: ['不可信请求', '拦截点', '策略状态', '检查/使用竞态'],
    before: [
      ['policy 与 mechanism 的区别？', 'policy 决定允许什么；mechanism 提供观察、限制与执行决定的手段。'],
      ['TOCTOU 指什么？', '检查某对象后到实际使用前，对象或其指向可能改变。'],
      ['路径名和 inode 是同一个对象吗？', '不是。路径是查找表达式，解析结果会受目录、链接和命名空间状态影响。']
    ],
    mental: '沙箱必须约束“最终被内核使用的对象”，而不只是检查用户最初给出的字符串。',
    reasoning: ['对象：系统调用参数、用户内存、路径解析状态、内核对象、策略状态。', '最小例子：检查 path 后，另一个线程替换软链接，open 使用了不同目标。', '机制链：复制/固定输入 → 在同一信任域解析 → 对最终对象执行策略。', '为什么不是字符串前缀检查：名字不是稳定对象身份。'],
    checks: [
      ['为什么系统调用拦截如此困难？', 'TOCTOU', Q('监控器先读取用户 path 指针并批准，内核稍后再次从该指针读取。风险是什么？', '用户线程可在两次读取间改写字符串', '字符串会被 CPU 自动加密', '系统调用编号会随机变化', '检查与使用面对的可能不是同一字节序列。', '这里没有自动加密机制。', '编号由 ABI 约定，不是这个竞态。', F('把 path 一次性复制进监控器可信内存主要修复什么？', '固定被检查的字节内容', '保证路径目标永远不变', '复制关闭了用户缓冲区竞态。', '目录或链接状态仍可能改变。'))],
      ['如何避免这些陷阱？', '对象身份', Q('为什么只允许以 /safe 开头的路径仍可能越界？', '符号链接或 .. 解析后可指向 /safe 之外', '因为绝对路径不能打开文件', '因为内核从不解析字符串', '词法前缀不等于最终文件对象。', '绝对路径是正常接口。', '内核必须完成路径解析。', F('更强的检查应尽量绑定什么？', '解析后的内核对象/受控目录句柄', '字符串长度', '稳定对象身份减少名称重解释。', '长度与目标权限无关。'))]
    ],
    transfer: Q('系统调用白名单能否完整描述一个调用的安全性？', '通常不能；参数、对象状态与调用序列同样重要', '能，只看编号即可', '不能，因为所有系统调用都应禁止', '安全语义常是有状态且依赖对象。', '同一 open 可访问不同路径。', '沙箱目标是受控能力，不是禁用一切。'),
    open: [
      ['构造一个路径检查的 TOCTOU 时间线。', '线程 A 检查 symlink→允许；线程 B 替换 symlink；线程 A 的 open 解析到敏感目标。'],
      ['为什么 capability 风格句柄有时比全局路径名更易约束？', '句柄绑定已授权对象与可执行操作，减少后续名称解析和环境变化。']
    ],
    explain: ['interposition 的可见性与控制力分别是什么？', 'TOCTOU 的两个 time 对应什么？', '为什么路径字符串不是对象身份？', '白名单为什么可能需要状态？', '复制用户参数修复了什么、没修复什么？'],
    bridge: ['Lab syscall · 实现并攻击策略边界', '../labs/syscall.html#workbook-stage-sandbox', '先建立 syscall 观测，再用路径、并发和攻击测试证伪过弱策略。']
  };

  bank['08-page-faults'] = {
    problem: '页表中的“暂时不存在”不必等于错误；内核怎样把 fault 变成惰性分配、COW 和按需加载的控制流？',
    map: ['内存访问', 'PTE/权限失败', 'fault handler', '补页或终止'],
    before: [
      ['page fault 是异常还是中断？', '它是由当前指令同步触发的异常，handler 能定位 faulting VA 与访问原因。'],
      ['虚拟页未映射一定意味着程序 bug 吗？', '不一定；可能是惰性分配、按需分页或合法 mmap 首次访问。'],
      ['COW 为什么先把页设为只读？', '让首次写入产生可拦截 fault，从而延迟复制到真正需要时。']
    ],
    mental: 'Page fault 是页表状态机的分支：同一个“访问失败”由软件元数据解释为补救、复制、载入或真正错误。',
    reasoning: ['对象：fault VA、访问类型、PTE flags、backing source、引用计数。', '最小例子：fork 后父子共享只读页；子首次写触发 fault，复制后各自可写。', '机制链：硬件拒绝 → 保存原因/地址 → handler 分类 → 更新映射/TLB → 重试指令。', '为什么不是预先复制全部：大量页可能永远不写，提前复制浪费时间和内存。'],
    checks: [
      ['写时复制（Copy-On-Write）Fork', '共享与写入', Q('COW fork 后父子都只读一个物理页。子写入时正确顺序是什么？', '分配新页→复制旧内容→改子 PTE→减少旧页引用→失效 TLB', '直接把共享 PTE 改成可写', '只增加引用计数并重试', '写者必须获得私有副本并同步翻译缓存。', '这会让子改到父仍在使用的页。', '引用计数不提供可写私有存储。', F('若旧页引用计数已经是 1，写 fault 可怎样优化？', '无需复制，直接把唯一映射恢复可写并失效 TLB', '仍必须复制两次', '没有其他共享者时可原地解除 COW。', '复制已不再保护任何共享者。'))],
      ['按需分页（Demand Paging）', 'backing source', Q('同样是 invalid PTE，handler 为什么不能统一分配零页？', '有的页应从文件/交换区载入，有的访问根本非法', '因为零页不能被读取', '因为 PTE 没有虚拟地址', '软件元数据决定缺页的语义与来源。', '零填充页当然可以读取。', 'fault CSR 会提供地址，PTE 由地址定位。', F('mmap 文件页首次读 fault 最关键的额外对象是什么？', '文件及页内偏移/backing mapping', '用户寄存器 a0 的旧值', 'handler 需要知道从哪里取内容。', '参数寄存器不等于持久 backing metadata。'))]
    ],
    transfer: Q('“fault 越少越好”是否总是正确？', '不是；fault 可用来避免从未需要的工作，关键是总成本', '是，任何 fault 都说明 bug', '不是，因为 fault 没有运行时成本', '惰性机制用少量 fault 换取更少分配/I/O/复制。', '合法缺页是设计的一部分。', 'trap 与补页都有明显成本。'),
    open: [
      ['画出 COW 页从 fork 到两个进程各自写入的状态变化。', '共享可读/ref=2→一方 fault→私有副本+旧页/ref=1→另一方可原地变可写或再复制。'],
      ['handler 如何区分“合法惰性页”与野指针？', '依靠 VMA/进程大小/映射元数据、访问类型和权限，而不是只看 PTE invalid。']
    ],
    explain: ['page fault 的同步性为何重要？', 'COW 为什么依赖只读映射与引用计数？', 'fault handler 需要哪些元数据分类原因？', '更新 PTE 后为什么要考虑 TLB？', '惰性机制用什么资源换什么资源？'],
    bridge: ['Lab cow · 把 fault 变成复制协议', '../labs/cow.html#workbook-stage-share', '从引用计数与只读共享开始，用 tiny fork/write 序列验证每个状态迁移。']
  };

  bank['09-superpages'] = {
    problem: '普通 4 KiB 页让映射灵活，却会放大 TLB 与页表成本；什么时候值得用更大粒度换覆盖能力？',
    map: ['工作集', '页大小', 'TLB reach/页表层级', '碎片与迁移'],
    before: [
      ['TLB reach 是什么？', 'TLB 可同时缓存的条目数乘每条映射覆盖的页大小。'],
      ['内部碎片来自哪里？', '分配粒度大于实际使用量，页内未使用空间仍被占用。'],
      ['透明大页为什么需要对齐？', '大页 PTE 覆盖固定的大范围，VA/PA 都需满足该粒度的边界。']
    ],
    mental: '超级页扩大一次翻译的覆盖范围，但也把分配、迁移和权限的最小粒度一起放大。',
    reasoning: ['对象：TLB entry、4 KiB page、2 MiB superpage、连续物理区间。', '最小例子：64-entry TLB 用 4 KiB 覆盖 256 KiB；用 2 MiB 覆盖 128 MiB。', '机制链：更大页→更大 TLB reach/更少页表项→更强连续性与碎片约束。', '为什么不是全部大页：小映射、稀疏权限和物理碎片会付出过高代价。'],
    checks: [
      ['TLB 与超级页动机', '覆盖范围', Q('64 个 TLB entry 全部映射 2 MiB 页，理论 reach 是多少？', '128 MiB', '128 KiB', '32 MiB', '64×2 MiB=128 MiB。', '你仍按 4 KiB 或单位换算错误。', '这是把乘积算少了四倍。', F('相同 entry 数改回 4 KiB 页，reach 是？', '256 KiB', '256 MiB', '64×4 KiB=256 KiB。', 'MiB 与 KiB 混淆。'))],
      ['今日论文：透明大页支持', 'promotion 条件', Q('把 512 个 4 KiB 页提升为 2 MiB 大页，除 VA 连续外还必须关注什么？', 'PA 连续/可重映射、权限一致、脏与引用状态', '文件名必须相同长度', 'CPU 核心数必须是 512', '一个大页 PTE 要代表同质且可连续解释的整段。', '名称不参与页表提升。', '核心数与映射粒度无关。', F('若其中一个 4 KiB 子页需不同权限，能否直接共享一个大页 PTE？', '通常不能，需拆分或放弃提升', '能，大页会保存 512 组权限', '一个叶 PTE 通常提供一组权限。', '你把大页当成内部仍有 512 个叶 PTE。'))]
    ],
    transfer: Q('大页降低 TLB miss 后，应用一定更快吗？', '不一定；promotion、碎片、NUMA 放置与内存带宽仍可能主导', '一定，TLB 是唯一瓶颈', '不一定，因为大页不会影响 TLB', '收益取决于 TLB miss 在总时间中的占比与新成本。', '这是单一瓶颈假设。', '大页的核心收益之一正是 TLB reach。'),
    open: [
      ['解释“页大小”同时影响哪三类资源。', '翻译覆盖/页表，物理分配与碎片，以及权限/回收/迁移粒度。'],
      ['构造一个不适合超级页的地址空间。', '大量稀疏小映射、细粒度不同权限、物理内存严重碎片或频繁局部回收。']
    ],
    explain: ['TLB reach 如何计算？', '大页为什么减少页表遍历与条目？', 'promotion 需要哪些一致性条件？', '大页为什么可能增加内部碎片？', '什么时候需要把大页 demote？'],
    bridge: ['Lab pgtbl · 超级页生命周期', '../labs/pgtbl.html#workbook-stage-superpages', '实现分配、映射、fork、部分释放与 demotion，并用 PTE 证据验证。']
  };

  bank['10-uservm'] = {
    problem: '虚拟内存不只属于内核；当应用获得 fault 与映射控制后，能构建哪些运行时机制，又必须承担哪些不变量？',
    map: ['应用地址空间', 'VM primitive', 'fault upcall', 'GC/迁移/持久化'],
    before: [
      ['mmap 建立的是数据复制还是映射关系？', '是 VA 范围到 backing object/物理页的关系，内容可按需进入内存。'],
      ['用户级 fault handler 仍需谁提供可信入口？', '内核必须安全地交付 fault 信息、切换到异常栈并验证返回。'],
      ['GC 为什么关心页保护？', '可用权限变化和 fault 低成本地发现首次写入或访问集合。']
    ],
    mental: '用户级 VM 把“页表状态变化”变成可编程事件：内核提供安全原语，运行时赋予它 GC、迁移等语义。',
    reasoning: ['对象：VMA、backing object、page protection、fault upcall、user exception stack。', '最小例子：把旧对象页设只读；第一次写 fault 记录 dirty set 后恢复可写。', '机制链：应用声明映射/保护→硬件检测→内核安全上送→用户 handler 更新策略。', '为什么不是轮询所有内存：fault 只在首次触碰时付费并精确定位页。'],
    checks: [
      ['应用级虚拟内存原语', '机制与策略', Q('内核向应用上送 page fault 时，哪项必须仍由内核保证？', 'handler 入口、异常栈和返回上下文不能被伪造破坏隔离', '应用 handler 一定会修复 fault', '所有页都必须立即载入', '可编程不等于放弃保护边界。', '应用可以选择终止或处理失败。', '这与按需机制相反。', F('用户 handler 再次 fault 时为何常需要独立异常栈？', '原执行栈可能就是 fault 原因，递归处理需可靠空间', '因为 CPU 没有寄存器', '异常栈打破对故障栈的依赖。', 'CPU 仍有寄存器，问题是内存可靠性。'))],
      ['用例 2：虚拟机/容器迁移', 'dirty tracking', Q('预复制迁移把页设为只读并捕获写 fault，主要想测量什么？', '哪些页在复制期间被重新写脏', '哪些线程调用了 malloc', '物理页的颜色', 'dirty set 决定哪些页必须重传。', '分配调用不是所有写入。', '页颜色与这里的语义无关。', F('写入率高于网络复制率时，预复制可能怎样？', '难以收敛，重复脏页不断需要重传', '立刻零停机完成', '工作集变化速度超过传输速度。', '这忽略了带宽与写入竞争。'))]
    ],
    transfer: Q('把 VM policy 移到用户态一定减少内核复杂度吗？', '内核机制可能更通用，但安全 upcall、资源核算与失败恢复仍复杂', '一定，内核不再参与', '一定增加性能且没有代价', '职责重新划分，不是复杂度凭空消失。', '页表控制与 trap 仍需内核。', '性能取决于 fault 频率与路径成本。'),
    open: [
      ['解释用户级 GC 如何把页保护当成 write barrier。', '先保护页，首次写触发 fault，handler 记录脏页并恢复权限，避免每次写都插软件检查。'],
      ['为什么用户级 fault API 是能力也是攻击面？', '它支持运行时创新，但必须验证入口、栈、地址范围与资源使用，防止绕过或 DoS。']
    ],
    explain: ['mmap 的对象关系是什么？', 'user-level fault handler 的信任边界在哪里？', '页保护如何充当事件检测器？', '预复制迁移为何可能不收敛？', '机制下放为什么不等于内核退出？'],
    bridge: ['Lab mmap · 构建映射状态机', '../labs/mmap.html#workbook-stage-map', '从 VMA contract、lazy fault、writeback 到 unmap/fork 逐步验证。']
  };

  bank['11-interrupts'] = {
    problem: '设备事件异步到来，CPU 不能一直轮询；中断怎样进入内核，同时避免在最脆弱的上下文里做太多工作？',
    map: ['设备状态', 'interrupt controller', 'top half', 'queue/bottom half'],
    before: [
      ['中断与 page fault 的触发关系有何不同？', '中断通常异步于当前指令；page fault 由当前内存访问同步触发。'],
      ['MMIO 是什么？', '把设备寄存器映射进物理地址空间，用 load/store 与设备交互。'],
      ['为什么 handler 不能随意睡眠？', '中断上下文可能没有可阻塞线程语义，且持有特殊锁/关闭中断。']
    ],
    mental: '中断路径负责“及时接住事件并建立所有权”，重工作应尽快转移到可调度上下文。',
    reasoning: ['对象：设备寄存器、pending bit、PLIC、CPU interrupt state、driver queue。', '最小例子：UART 收到字节→中断→handler 读寄存器清事件→把字节入软件缓冲。', '机制链：设备置位→控制器路由→trap→ack/搬最小状态→唤醒后续处理。', '为什么不是 handler 做全部协议：会拉长中断关闭时间并阻塞其他事件。'],
    checks: [
      ['中断：设备通知 CPU 的机制', 'ack 顺序', Q('handler 忘记清除/确认设备中断，最典型后果是什么？', '返回后同一中断立即再次触发，形成 interrupt storm', '设备自动变成只读', '页表被清空', 'pending 原因仍存在，控制器会继续报告。', '中断确认与寄存器权限无此必然关系。', '设备中断不会自动重建页表。', F('若先 ack 再读取一个会被覆盖的单槽数据寄存器，风险是什么？', '新事件可能覆盖旧数据，造成丢失', 'CPU 会自动回滚 ack', '顺序必须匹配设备协议。', '设备通常不提供这种自动事务回滚。'))],
      ['通用驱动模式：Bottom-half 与 Top-half', '拆分工作', Q('top half 最适合做什么？', '确认事件、保存最小必要状态并安排后续工作', '执行任意长的文件系统操作', '等待用户进程提供缓冲区', '它应缩短不可抢占/高优先级路径。', '长工作扩大中断延迟。', '中断上下文不能依赖某个用户线程及时运行。', F('bottom half 的主要优势是什么？', '可在更普通的调度上下文处理较重工作', '保证设备永不丢包', '拆分改善调度与延迟，但不自动保证容量。', '队列溢出仍可能发生。'))]
    ],
    transfer: Q('把中断合并得越多是否总越好？', '不是；减少每事件开销，但会增加单个事件等待延迟', '是，中断没有延迟要求', '不是，因为合并会增加 CPU 指令频率', '这是吞吐与尾延迟的 trade-off。', '交互和控制事件常有延迟约束。', '合并通常减少而非增加中断频率。'),
    open: [
      ['画出 UART 接收一个字节的硬件到软件路径。', 'UART 寄存器→PLIC→CPU trap→driver ack/read→软件队列→wakeup→进程 read。'],
      ['为什么中断 handler 的共享队列需要同步？', '生产者在中断上下文，消费者在进程/其他 CPU 上；读写索引和数据可并发。']
    ],
    explain: ['中断与同步异常为什么不能混为一谈？', 'MMIO load/store 与普通 RAM 访问有哪些语义差异？', 'ack 顺序为何属于设备 contract？', 'top/bottom half 各自负责什么？', '中断合并交换了哪些指标？'],
    bridge: ['Lab traps / net · 接住异步事件', '../labs/traps.html#workbook-stage-alarm', '先掌握 trap 上下文保存，再到网络环形队列验证设备所有权。']
  };

  bank['12-locking'] = {
    problem: '并发执行让临界区交错；锁怎样保护不变量，而不是仅仅“让一段代码一次一个线程”？',
    map: ['共享对象', '不变量', '临界区', '原子性/顺序'],
    before: [
      ['data race 和不变量破坏的关系？', '冲突访问可能观察中间状态并丢失更新；锁应覆盖维持不变量所需的整组状态转换。'],
      ['原子指令为何仍需内存顺序？', '取得锁的原子性不自动保证临界区读写在其他 CPU 看来按预期可见。'],
      ['关闭本 CPU 中断能阻止其他 CPU 吗？', '不能，只阻止本 CPU 被本地中断 handler 重入。']
    ],
    mental: '锁保护的是跨多字段、跨多步的对象不变量；临界区边界应从不变量推导，而不是从代码行数猜。',
    reasoning: ['对象：共享状态、owner、lock word、临界区前后不变量。', '最小例子：freelist 的 head 与 next 必须作为一个状态转换更新。', '机制链：acquire 建立独占/可见性→更新对象→恢复不变量→release 发布。', '为什么不是每个字段各一把锁：跨字段不变量可能在两把锁之间暴露中间状态。'],
    checks: [
      ['何时需要锁的保守规则', '锁的对象', Q('链表插入要改 new.next 和 head。只锁 head 写入那一行足够吗？', '不一定；必须覆盖从读取旧 head 到发布新 head 的完整状态转换', '足够，只有写指令才会 race', '不需要锁，因为指针写是原子的', '正确性依赖组合操作的线性化。', '你把单条写的原子性误当成操作原子性。', '原子指针写不能防止丢失并发更新。', F('判断临界区边界最可靠的问题是什么？', '不变量在哪一步恢复、何时可让其他线程观察', '代码块有多少行', '边界来自对象语义。', '行数与共享状态无固定关系。'))],
      ['如何实现锁？', 'acquire/release ordering', Q('原子交换成功后为何还需要 acquire 语义？', '阻止后续临界区访问被重排到取得锁之前', '让锁变量占用更少内存', '自动避免死锁', '同步既需所有权原子性，也需可见性顺序。', '内存大小与 ordering 无关。', '死锁由多锁依赖等更高层结构决定。', F('release 语义要保证什么？', '临界区写入在其他线程取得锁前可见', '释放后本线程永不再运行', '发布顺序把对象状态交给下一 owner。', '调度不由 release 保证。'))]
    ],
    transfer: Q('无 data race 的程序是否一定无死锁？', '不是；锁顺序循环可在完全同步的程序中死锁', '是，race 与 deadlock 是同一问题', '不是，因为锁从不提供互斥', '安全性与活性是不同性质。', '你混淆了并发错误类别。', '锁的基本作用正是互斥。'),
    open: [
      ['用 happens-before 解释一次锁交接。', '线程 A 临界区写→release；线程 B 后续 acquire→读取，形成可见性顺序。'],
      ['构造两把锁的最小死锁，并给出修复原则。', 'A 持 L1 等 L2，B 持 L2 等 L1；规定全局锁顺序。'],
      ['为什么锁粒度既影响正确性也影响性能？', '太细难覆盖不变量且增加协议；太粗扩大竞争和串行部分。']
    ],
    explain: ['锁保护的对象为何是不变量？', '原子交换与内存顺序分别解决什么？', '关闭中断和跨 CPU 互斥有什么区别？', 'data race、deadlock 与 starvation 如何区分？', '锁粒度为什么不是越细越好？'],
    bridge: ['Lab lock · 用 profile 证据拆锁', '../labs/lock.html#workbook-stage-measure', '先证明共享状态正确，再用 contention 证据选择 per-CPU 或更细粒度结构。']
  };
})();
