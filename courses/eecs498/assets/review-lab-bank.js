(function () {
  const source = 'https://github.com/PointBreaker/EECS498/tree/1b495ae8c4db1343a12a39ac626a0a84dcada300/';
  const lesson = (number, slug, label) => ({ label: `Lesson ${number} · ${label}`, href: `../lessons/${String(number).padStart(4, '0')}-${slug}.html` });
  const stage = (id, title, build, why, lessons, contract, readiness, sanity, failures, prediction, experiment, evidence, hints, gate) => ({ id, title, build, why, lessons, contract, readiness, sanity, failures, prediction, experiment, evidence, hints, gate });
  window.EECS498ReviewLabs = {
    a1: {
      eyebrow: 'A1 · TENSOR FOUNDATIONS', title: '从 tensor 操作到可信实验', subtitle: '不重写当年的作业；用小输入、shape ledger 与证据重新审计 KNN 和向量化。',
      source: `${source}A1`, capabilities: ['Tensor contract', 'Vectorization', 'Experimental evidence'],
      stages: [
        stage('tensor-contract', 'Tensor Contract', '能逐轴解释 batch、feature 与 class，而不是靠广播碰巧运行。', '后面的每一个模型都建立在 shape、dtype、device 与 broadcasting 的可预测性上。', [lesson(1,'neural-networks-are-functions','函数链')], {input:'X_train [N,D], X_test [M,D]',output:'distance [M,N]',invariants:['每个单元只对应一对样本','结果非负，完全相同输入距离为 0'],forbidden:['把 batch 轴当 feature 轴','依赖隐式广播但说不出扩张方向']}, ['能解释 [M,1,D] 与 [1,N,D] 如何广播','能区分 view/reshape 与数据复制'], '取 M=2,N=3,D=2，用纸手算六个平方距离，再与代码逐项比较。', [{see:'shape 正确但每行完全相同',check:'广播轴或 reduction dimension'},{see:'距离出现负数',check:'平方和公式、数值误差与是否漏掉 square'}], '把 one-loop 改成 no-loop 后，数值应该一致；运行时间会下降，但临时 tensor 可能增大。', '固定同一小输入，比较 two-loop / one-loop / no-loop 的最大绝对误差与 wall time。', ['三种实现误差','输入 shape/dtype/device','是否同步计时、重复次数'], ['先写出目标 distance matrix 的每个轴','检查 sum 的 dimension，而不是先改公式','把输入缩到 2×3×2 并打印广播后的 shape'], ['三种实现逐项一致','能闭卷解释广播','测量记录包含配置']),
        stage('knn-decision', 'KNN Decision Rule', '让距离矩阵经过 top-k 与多数表决产生确定预测。', '距离计算正确不代表分类正确；tie-break、axis 与 label dtype 都属于算法契约。', [lesson(1,'neural-networks-are-functions','函数链')], {input:'distance [M,N], y_train [N]',output:'y_pred [M]',invariants:['每个 test example 独立投票','平票规则确定且可复现'],forbidden:['在训练样本轴外取 top-k','依赖 unordered 容器决定平票']}, ['能解释 k 改变 bias/variance','能区分最近样本索引与样本标签'], '构造三个训练点，令两个类别 1:1 平票；先写出预期 tie-break 再运行。', [{see:'所有测试样本预测相同',check:'top-k 的 axis 与 batch indexing'},{see:'重复运行偶尔改变答案',check:'平票规则或无序集合'}], 'k=1 对局部噪声敏感；k 增大通常更平滑，但过大可能吞掉局部结构。', '在固定 split 上画 k 与 validation accuracy，不使用 test set 选 k。', ['每个 k 的 validation accuracy','固定 split/seed','最终 test 只评一次'], ['先确认 nearest indices 的 shape','将 labels 和 indices 同时打印','对单个 test example 手推投票'], ['tiny tie case 确定','validation 选择与 test 评估分离','能解释 k 的取舍']),
        stage('evidence', 'Evidence Gate', '把“代码跑通”升级成可复现实验结论。', 'Notebook 输出若缺配置、基线和分离的数据集，只能说明一次运行发生过。', [lesson(3,'optimization-training-dynamics','训练动力学')], {input:'实现 + 数据 split + 配置',output:'可复现结论',invariants:['验证集用于选择，测试集用于最终报告','比较只改变一个主要变量'],forbidden:['用 test accuracy 反复调参','只报告最好数字不报告条件']}, ['能说明 baseline 是什么','知道随机种子不等于完全确定性'], '故意交换 validation/test 的角色，解释为什么结果会乐观且不可复用。', [{see:'每次结果差异很大',check:'split、seed 与并列投票'},{see:'validation 很高但 test 明显掉点',check:'是否对 validation 过拟合'}], '向量化应改变速度而非预测；k 改变预测而非距离。', '建立 2×2 ablation：loop/no-loop × 两个 k，分别记录数值一致性、速度和 accuracy。', ['baseline','单变量变化','结果与机制解释'], ['先把“实现优化”和“模型超参数”分栏','预测哪些指标应该不变','若结论反常，先检查控制变量'], ['能从证据区分性能优化与算法变化','记录足够复现','写出一条被数据否定的原预测'])
      ]
    },
    a2: {
      eyebrow:'A2 · CLASSIFIERS', title:'从 score 到可学习 representation', subtitle:'沿 linear classifier、softmax 与 two-layer net 审计 forward、loss、gradient 和 update 的完整闭环。',
      source:`${source}A2`, capabilities:['Loss derivation','Gradient checking','Training diagnosis'],
      stages:[
        stage('scores-loss','Scores and Loss','让 XW 产生 class scores，再由 SVM/softmax 把错误压成标量。','没有标量 objective，就没有统一的优化方向。',[lesson(1,'neural-networks-are-functions','函数链'),lesson(2,'backprop-computational-graphs','反向传播')],{input:'X [N,D], W [D,C], y [N]',output:'scores [N,C], scalar loss',invariants:['每个样本只有正确类别索引','regularization 只约束参数'],forbidden:['把 score 当 probability','在 batch sum 后忘记除 N']},['会从 shape 推导 XW','能解释数值稳定 softmax 为什么减 max'],'用 N=2,C=3 的整数 scores 手算 softmax probability 与平均 loss。',[{see:'loss 随统一平移 scores 改变',check:'softmax 稳定化/归一化'},{see:'batch 翻倍 loss 也翻倍',check:'是否取平均'}],'同一行 scores 加常数不改变 softmax；增大正确类相对 margin 应降低 loss。','对固定 scores 做两次受控扰动并记录 loss 变化。',['原始/扰动 scores','概率和是否为 1','loss 变化方向'],['先算 exp 前的相对差','检查 softmax dimension','把 batch 缩成两行'],['手算与实现一致','能解释 invariance','loss 对正确方向响应']),
        stage('gradient','Gradient Contract','证明 vectorized gradient 与数值梯度、naive 实现一致。','向量化可能保持 shape 却把样本轴和类别轴混掉。',[lesson(2,'backprop-computational-graphs','反向传播')],{input:'loss(W)',output:'dW [D,C]',invariants:['dW 与 W 同 shape','有限差分误差随 epsilon 在合理区间下降'],forbidden:['只凭 loss 下降认定梯度正确','在含 ReLU kink 点机械做 gradient check']},['能写中心差分','知道 relative error 比 absolute error 更可比'],'用 2×3 的 W 只检查三个指定元素，不先跑全矩阵。',[{see:'部分列符号反了',check:'正确类别修正项'},{see:'误差对 epsilon 极敏感',check:'非光滑点或浮点消减'}],'naive/vectorized loss 和 gradient 应一致；vectorized 只改变执行方式。','在相同输入上记录 loss 差、dW relative error、运行时间。',['epsilon','dtype','最大误差位置'],['先比较 loss 再比较 gradient','定位最大误差的索引','对该索引展开一个样本的贡献'],['relative error 达到预期','能解释误差来源','vectorization 不改变语义']),
        stage('two-layer','Two-layer Representation','审计 affine→ReLU→affine 的参数、activation 与 gradient。','ReLU 是两层不坍缩成一次 affine 的关键。',[lesson(1,'neural-networks-are-functions','函数链'),lesson(3,'optimization-training-dynamics','训练动力学')],{input:'X [N,D]',output:'scores [N,C]',invariants:['W 不带 batch 轴','ReLU mask 只由 forward preactivation 决定'],forbidden:['把 hidden activation 当 parameter','backward 重新计算不同 mask']},['能区分 parameter/activation/gradient','会追 [N,D]→[N,H]→[N,C]'],'取 N=2,D=3,H=2,C=2，设置一个隐藏单元恒为负，预测其梯度路径。',[{see:'W1 完全不更新',check:'ReLU mask 与链式法则'},{see:'训练/验证差距快速扩大',check:'capacity、regularization 与数据'}],'没有 ReLU 时两层可合并；加入 ReLU 后不同样本可走不同局部线性区域。','同一初始化比较有/无 ReLU 的 gradient pattern 与训练曲线。',['相同 seed/init','激活稀疏率','loss 曲线'],['打印 preactivation 与 mask','确认 cache 来自同一次 forward','先检查一条样本路径'],['shape 与 cache 正确','能解释非线性','证据支持局部路径差异']),
        stage('training-gate','Training Evidence','用 loss、accuracy、gradient norm 和基线共同解释训练。','单一 accuracy 无法区分没学会、过拟合或数据泄漏。',[lesson(3,'optimization-training-dynamics','训练动力学')],{input:'model + optimizer + split',output:'可解释训练轨迹',invariants:['训练配置完整记录','test 不参与调参'],forbidden:['只保存最好模型不记录过程','把随机波动写成优化结论']},['会建立 random/chance baseline','知道 loss 降而 accuracy 暂不变并非矛盾'],'在十个样本上尝试过拟合到接近 100%；失败时按 shape→gradient→update 排查。',[{see:'loss 恒定',check:'gradient 是否非零、参数是否更新'},{see:'train 高 val 低',check:'overfit、regularization、split'}],'正确实现应能过拟合极小数据；全量数据表现则受泛化影响。','先 tiny overfit，再固定配置跑完整 split。',['tiny loss/accuracy','gradient norm','train/val curves'],['检查参数前后差值','关闭复杂 augmentation','一次只检查一个模块'],['tiny data 可过拟合','完整轨迹可解释','结论不依赖 test 调参'])
      ]
    },
    a3: {
      eyebrow:'A3 · DEEP NETWORKS', title:'模块化反向传播与卷积网络', subtitle:'把 layer cache、optimizer state、normalization 与 convolution 当成可审计的对象生命周期。', source:`${source}A3`, capabilities:['Layer contracts','Optimizer state','CNN debugging'],
      stages:[
        stage('layers','Layer Contracts','为 affine、ReLU、normalization 建立 forward/backward 对偶契约。','模块化只在 cache 身份和 shape 可追踪时才减少复杂度。',[lesson(2,'backprop-computational-graphs','反向传播')],{input:'x,w,b + upstream dout',output:'out + dx,dw,db',invariants:['每个 gradient 对应原对象 shape','backward 使用同一次 forward cache'],forbidden:['用全局可变状态偷传 cache','广播后忘记按被广播轴求和']},['能解释 upstream gradient','会推 bias gradient reduction'],'用 N=2,D=3,C=2 手推 affine backward 的三个输出 shape。',[{see:'db 带 batch 维',check:'broadcast reduction'},{see:'单层对、组合错',check:'cache 顺序与接口'}],'组合层的数值梯度应与逐层链式组合一致。','对 affine-ReLU composite 做 sparse gradient check。',['每个参数 relative error','非光滑点处理','输入规模'],['先测独立 layer','再测 sandwich','打印 cache 类型与 shape'],['每层独立通过','组合通过','能从计算图解释三条梯度']),
        stage('optimizers','Optimizer State','区分 gradient、momentum、second moment 与 parameter update。','Adam/SGD 的差异来自状态和缩放，不是“更聪明”的标签。',[lesson(3,'optimization-training-dynamics','训练动力学')],{input:'w,dw,config',output:'next_w,next_config',invariants:['状态 shape 与参数一致','step 计数单调且每次只增一次'],forbidden:['跨参数共享同一 mutable config','把 weight decay 与 L2 gradient 永远视为等价']},['能手算一维 momentum','能区分优化状态与模型参数'],'一个参数 w=1、g=2，手算两步 momentum，再运行实现。',[{see:'第二步像第一步',check:'状态没有持久化'},{see:'不同参数互相污染',check:'config aliasing'}],'momentum 在同向梯度下累积速度；梯度反向时会有惯性。','用预设梯度序列 [2,2,-2] 比较 SGD/momentum/Adam 的 update。',['每步 state','每步 delta w','超参数'],['先用标量','打印 state identity','检查 step 增量位置'],['标量轨迹一致','能解释状态记忆','不同参数状态隔离']),
        stage('convolution','Convolution Shape and Locality','沿 N,C,H,W、filter 与 stride/pad 推导输出及反向。','CNN 把空间局部性和权重共享写进参数结构。',[lesson(4,'cnn-spatial-structure','CNN 空间结构')],{input:'x [N,C,H,W], w [F,C,HH,WW]',output:'out [N,F,Hout,Wout]',invariants:['同一 filter 在所有位置共享','输出尺寸满足整数滑动窗口'],forbidden:['把 channel 当空间轴','只记公式不检查边界窗口']},['会从一个窗口手算 dot product','能解释 parameter sharing'],'N=C=F=1，3×3 输入配 2×2 filter，stride=1，无 padding，手算 2×2 输出。',[{see:'输出错一格',check:'边界与 stride 起点'},{see:'每个位置参数不同',check:'是否丢失权重共享'}],'stride 增大降低空间分辨率；padding 可保持尺寸但引入边界假设。','固定 filter 比较 stride/pad 四种配置的 shape 与一个角落值。',['公式预测','实际 shape','边界值来源'],['先画窗口坐标','确认 NCHW 次序','只看左上输出单元'],['toy 输出正确','shape 预测正确','能解释共享与局部性']),
        stage('cnn-gate','CNN Evidence Gate','用 activation、gradient 与 timing 解释网络是否正确且值得。','更深或更快都不是单一结论；必须知道改变了什么资源和表示。',[lesson(4,'cnn-spatial-structure','CNN 空间结构'),lesson(5,'vision-representations','视觉表示')],{input:'baseline CNN + one controlled change',output:'correctness/performance evidence',invariants:['相同数据与训练预算','同时记录模型行为和运行配置'],forbidden:['把 wall time 差异归因于 kernel 而不 warmup','只挑最好一次']},['知道 GPU timing 需同步','能解释 activation distribution'],'先让单 batch 过拟合，再比较一个 architecture change。',[{see:'train loss NaN',check:'normalization、learning rate、gradient norm'},{see:'速度反而慢',check:'shape、warmup、kernel overhead'}],'改变 normalization 可能改善训练稳定性，但不会自动减少 FLOPs。','固定 seed/batch/steps，记录 loss、gradient norm、latency。',['baseline/config','重复次数','机制解释'],['正确性先于 benchmark','tiny overfit 后再 profile','用 profiler 验证瓶颈'],['能过拟合小 batch','性能测量条件完整','结论区分优化与统计效果'])
      ]
    },
    a4: {
      eyebrow:'A4 · REPRESENTATIONS & SEQUENCES', title:'从可视化到序列记忆', subtitle:'审计 autograd、style loss、RNN/LSTM/attention，并把历史实现连到 Transformer。', source:`${source}A4`, capabilities:['Autograd reasoning','Sequence state','Attention transfer'],
      stages:[
        stage('autograd','Autograd Boundary','辨认哪些 tensor 需要 gradient，哪些只是固定输入或 target。','错误的 requires_grad 边界会浪费显存或切断优化链。',[lesson(5,'vision-representations','视觉表示')],{input:'image/features/targets',output:'scalar objective + selected gradients',invariants:['只优化目标对象','loss 由可微路径连接到目标'],forbidden:['对 target 累积无用 gradient','在中途 detach 优化对象']},['能解释 leaf tensor','知道 zero_grad 与 detach 不同'],'用 x² 的两步优化观察 gradient accumulation，然后显式清零。',[{see:'第二步 gradient 异常变大',check:'是否清零'},{see:'loss 变化但目标不更新',check:'optimizer 参数与 leaf'}],'若只优化输入图像，模型权重应固定而图像有 gradient。','记录一次 backward 后各类 tensor 的 grad 是否存在。',['requires_grad','is_leaf','grad norm'],['先列优化对象','检查 optimizer param list','沿 grad_fn 回溯'],['gradient 边界正确','无意外参数更新','能解释累积']),
        stage('rnn','Recurrent State','沿时间追 hidden state 与 shared weights。','RNN 的参数共享跨时间，但 activation 和梯度路径随 T 增长。',[lesson(7,'rnn-through-time','RNN')],{input:'x [N,T,D], h0 [N,H]',output:'h [N,T,H]',invariants:['同一 Wx/Wh 每步共享','第 t 状态只依赖过去'],forbidden:['为每个时间步创建新参数','把 time 与 batch 轴互换']},['会手推两步 recurrence','能区分 h_t activation 与 Wh parameter'],'N=1,T=2,D=H=1，给定标量权重手算 h1,h2。',[{see:'不同 T 参数量改变',check:'是否错误创建 per-step weight'},{see:'未来 token 影响过去',check:'时间方向或 mask'}],'T 增大参数量不变，但串行路径和 memory 增长。','比较 T=8/16 的参数量、activation 数和 latency。',['参数量','shape','运行时间'],['先展开两步','打印每步 h','确认 weight identity'],['toy recurrence 对','参数跨步共享','能解释串行瓶颈']),
        stage('lstm-attention','LSTM and Attention','区分门控记忆路径与基于内容的动态读取。','LSTM 改善梯度通路，attention 改变信息访问方式。',[lesson(8,'lstm-memory','LSTM'),lesson(10,'transformer-attention-routing','Attention')],{input:'sequence states + query',output:'updated state or weighted context',invariants:['attention weights 在 key 轴归一化','LSTM cell/hidden 身份不混淆'],forbidden:['把 attention 当固定平均','认为 LSTM 消除全部长程问题']},['能解释 sigmoid gate 范围','会从 score 得到 weighted sum'],'给三个 scalar values 和权重 [0.2,0.3,0.5] 手算 context，再改变 query 解释权重变化。',[{see:'权重和不为 1',check:'softmax axis'},{see:'所有 query 得到同一 context',check:'score 是否依赖 query'}],'attention 可并行比较位置；RNN/LSTM 必须沿 recurrence 顺序产生 state。','固定表示，比较 recurrent update 与 attention read 的依赖图和 latency 趋势。',['依赖图','权重分布','T 增长趋势'],['先区分 state update 与 memory read','检查 softmax 轴','用两个不同 query'],['context 手算正确','能解释两种机制边界','连接到 Transformer QKV']),
        stage('sequence-gate','Sequence Evidence Gate','用 teacher forcing、mask 与 generation trace 解释序列模型。','训练 loss 正确不代表自回归生成路径正确。',[lesson(12,'transformer-seq2seq-training','Seq2Seq 训练'),lesson(13,'transformer-to-modern-llms','现代 LLM')],{input:'tokens/mask/targets',output:'loss + generated sequence',invariants:['target shift 一致','生成时不能读取未来 token'],forbidden:['训练和生成使用不同 token convention','用完整 target 泄漏未来']},['会解释 teacher forcing','能画 causal dependency'],'构造长度 3 序列，逐位置写 input、target 与允许 attention 的位置。',[{see:'训练极好生成崩坏',check:'shift/mask/解码路径'},{see:'第一个 token loss 异常',check:'BOS 与 label 对齐'}],'正确 causal mask 下，改变未来 token 不应影响过去 logits。','复制输入只改最后 token，比较前面位置 logits。',['mask','logit max diff','token convention'],['先画三角 mask','逐位置对齐 labels','检查 generation 第一步'],['future-invariance 通过','shift 可解释','训练与推理路径一致'])
      ]
    },
    a5: {
      eyebrow:'A5 · DETECTION', title:'从 dense feature 到结构化预测', subtitle:'用 anchor、IoU、matching、loss 与 NMS 审计检测系统的对象关系。', source:`${source}A5`, capabilities:['Box geometry','Matching invariants','Detection evaluation'],
      stages:[
        stage('geometry','Box Geometry','让坐标约定、面积与 IoU 在 tiny boxes 上完全确定。','大多数检测 bug 先表现为几何约定错，而不是网络不够强。',[lesson(6,'detection-structured-prediction','结构化预测')],{input:'boxes [N,4]',output:'IoU [N,M]',invariants:['IoU ∈ [0,1]','无交集时 intersection 为 0'],forbidden:['混用 xyxy 与 xywh','宽高为负仍继续计算']},['能手算两个方框交并面积','知道像素闭区间约定影响 +1'],'手算 [0,0,2,2] 与 [1,1,3,3] 的 IoU，并注明坐标约定。',[{see:'IoU 大于 1',check:'union/intersection 或负宽高'},{see:'相同框 IoU 不是 1',check:'面积约定'}],'相同框 IoU=1，完全分离=0，部分重叠介于两者。','建立三组 boxes 作为 geometry regression。',['坐标 convention','手算值','实现值'],['先画平面','clamp intersection','独立测试 area'],['三个边界 case 通过','约定写入 contract','矩阵轴可解释']),
        stage('matching-loss','Matching and Loss','把 anchors 分成 positive/negative/ignore，并分别计算分类和回归损失。','错误 matching 会让模型从矛盾标签学习。',[lesson(6,'detection-structured-prediction','结构化预测')],{input:'anchors, GT boxes/classes',output:'matched targets + losses',invariants:['回归只作用于 positive','每个 target 规则确定'],forbidden:['让 ignore 样本进入分类损失','按预测分数而非 GT 匹配']},['会解释 threshold 区间','能区分 classification 与 box target'],'给 4 个 anchors 的 IoU [0.8,0.55,0.3,0.05]，按给定阈值手工分类。',[{see:'box loss 被大量背景主导',check:'positive mask'},{see:'正样本数量为 0',check:'阈值与 best-match 规则'}],'提高 positive threshold 通常减少正样本并提高匹配质量，但可能降低 recall。','固定预测，只改变 matching threshold，记录样本数与 loss 组成。',['positive/ignore/negative count','loss components','threshold'],['先只输出 labels','再检查 regression targets','最后接 loss'],['匹配可手推','loss mask 正确','能解释阈值取舍']),
        stage('nms','NMS Decision','让 score sorting 与 IoU suppression 产生确定候选集合。','NMS 是离散决策，不属于训练网络本身，却直接改变 precision/recall。',[lesson(6,'detection-structured-prediction','结构化预测')],{input:'boxes + scores',output:'kept indices',invariants:['先保留高分候选','suppression 只比较指定类别策略'],forbidden:['按输入顺序处理','把 NMS threshold 当置信度 threshold']},['会手推三框 NMS','能区分 class-aware/class-agnostic'],'三个框中两个高度重叠，第三个独立；写出不同 IoU threshold 的保留集合。',[{see:'低分框压掉高分框',check:'排序方向'},{see:'不同类别互相删除',check:'NMS 是否应 class-aware'}],'降低 NMS IoU threshold 会更激进抑制，可能提高 precision、降低 recall。','固定模型输出 sweep 两个 NMS threshold。',['kept count','precision/recall','类别策略'],['先排序','每轮只保留一个最高分','打印被谁抑制'],['toy trace 正确','阈值效果可解释','结果确定']),
        stage('detector-gate','Detector Evidence Gate','用错误类型而非单一 mAP 解释检测器。','mAP 聚合了分类、定位、漏检与重复框，不能单独定位 bug。',[lesson(6,'detection-structured-prediction','结构化预测')],{input:'predictions + GT + evaluation config',output:'metric + error evidence',invariants:['坐标尺度一致','metric 配置与类别集合记录'],forbidden:['只看一张好图','在不同 NMS/score threshold 下直接比 mAP']},['会区分 FP/FN/localization error','知道 confidence threshold 改变 operating point'],'挑一张图手工标注每个 prediction 属于 TP/duplicate/localization/background。',[{see:'可视化看似对但 mAP 低',check:'坐标缩放、类别 id、evaluation config'},{see:'很多重复框',check:'NMS 与 matching'}],'若主要问题是定位，放宽 evaluation IoU 会显著改善；若是分类则不会。','在两个 evaluation IoU 下分解错误，并查看代表样本。',['metric config','error counts','representative evidence'],['先验证坐标反变换','再拆分类与定位','最后调 threshold'],['metric 可复现','至少三类错误有证据','提出针对机制的下一实验'])
      ]
    },
    a6: {
      eyebrow:'A6 · GENERATIVE MODELS', title:'VAE 与 GAN：两个不同的生成契约', subtitle:'分别审计显式变分目标与对抗博弈，不把“能生成图片”当作同一种机制。', source:`${source}A6`, capabilities:['ELBO accounting','Reparameterization','Adversarial diagnostics'],
      stages:[
        stage('vae-objective','VAE Objective','拆开 reconstruction 与 KL，并说清每一项约束谁。','两项失衡会导致只重建或只贴近 prior。',[lesson(14,'generative-models','生成建模')],{input:'x, μ, logvar, reconstruction',output:'scalar ELBO loss',invariants:['reconstruction 与 KL 分项记录','batch/reduction convention 一致'],forbidden:['只报告 total loss','把 KL 符号写反']},['能解释 approximate posterior','会手算 μ=0,logvar=0 的 KL'],'单维 μ=0,logvar=0 时手算 KL；再令 μ=1 比较。',[{see:'KL 为负',check:'公式符号/reduction'},{see:'KL 很快接近 0 且 decoder 忽略 z',check:'posterior collapse'}],'增大 KL 权重会更强约束 latent，但可能牺牲 reconstruction。','固定模型与 batch，比较两个 KL weight 的分项轨迹。',['recon','KL','latent statistics'],['先单维检查','分项打印','确认 logvar 不是 std'],['KL 边界正确','分项可复现','能解释 trade-off']),
        stage('reparameterization','Reparameterization','让随机采样保留从 loss 到 μ/logvar 的可微路径。','直接从参数化分布采样会让普通反向传播无法穿过随机节点。',[lesson(14,'generative-models','生成建模'),lesson(2,'backprop-computational-graphs','反向传播')],{input:'μ, logvar, ε~N(0,I)',output:'z=μ+σ⊙ε',invariants:['ε 不需 gradient','σ 由 logvar 正确转换'],forbidden:['把 logvar 直接当 σ','在 z 上 detach']},['能画随机节点计算图','知道 exp(0.5 logvar) 的来源'],'μ=2,logvar=0,ε=-1 时手算 z，并预测 dz/dμ。',[{see:'μ/logvar grad 为 None',check:'detach/no_grad'},{see:'variance 爆炸',check:'logvar→std 转换'}],'固定 ε 时 z 对 μ 的局部导数为 1；随机性来自多次 ε。','固定 seed 做 gradient check，再换 seed 看样本变化。',['ε','z','μ/logvar grad'],['打印 grad_fn','固定 epsilon','单元素开始'],['toy z 正确','gradient path 存在','能解释随机与可微并存']),
        stage('gan-game','Adversarial Game','分开 discriminator 与 generator 的目标、参数和 update。','GAN 是动态博弈；一个网络的改进会改变另一个网络看到的 objective。',[lesson(14,'generative-models','生成建模')],{input:'real x, noise z',output:'D/G losses + samples',invariants:['每步只更新目标网络参数','fake 在 D/G step 的 gradient 边界正确'],forbidden:['两个 optimizer 同时更新全部参数','只看一方 loss 判断成功']},['能解释 non-saturating G loss','会区分 detach 的位置'],'一维参数分别属于 G/D，逐步写出 D step 与 G step 哪个 gradient 应存在。',[{see:'G 从不变化',check:'fake 是否在 G step 也 detach'},{see:'D/G 参数同时变化',check:'optimizer param list/zero_grad'}],'D 太强可能让 G 梯度差；loss 平衡不保证样本多样。','记录两方 loss、gradient norm 与固定 noise samples。',['D/G loss','D/G grad norm','fixed-noise grid'],['先断言参数集合不交叉','每步比较参数 diff','固定噪声观察模式'],['更新边界正确','样本轨迹可比较','不以单一 loss 下结论']),
        stage('generative-gate','Generative Evidence Gate','用重建、采样、多样性与失败模式比较 VAE/GAN。','不同目标没有一个共享的“训练 loss 越低越好”标尺。',[lesson(14,'generative-models','生成建模')],{input:'trained models + fixed eval protocol',output:'mechanism-aware comparison',invariants:['固定输入/噪声便于纵向比较','同时检查质量与覆盖'],forbidden:['用 VAE loss 与 GAN loss 数值直接比较','只挑最好样本']},['能解释 blurry reconstruction 与 mode collapse','知道 qualitative evidence 需要固定 protocol'],'为 VAE 固定输入重建，为 GAN 固定 noise，写出各自最可能暴露的失败。',[{see:'样本漂亮但高度重复',check:'mode collapse/coverage'},{see:'重建模糊但 latent 平滑',check:'像素 likelihood 与目标取舍'}],'VAE 通常有可解释 objective 与覆盖倾向；GAN 可能更锐利但训练/覆盖更难。','用固定网格、latent interpolation 与最近邻检查建立证据包。',['固定协议','失败样本而非只放最好','目标边界解释'],['先定义成功标准','每种模型用适合它的证据','加入反例样本'],['比较不混用 loss 尺度','质量与覆盖都有证据','能解释两种机制为何不同'])
      ]
    }
  };
})();
